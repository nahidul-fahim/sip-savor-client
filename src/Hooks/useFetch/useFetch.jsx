import { useEffect, useState } from "react";


const useFetch = url => {

    const [output, setOutput] = useState([]);

    useEffect(() => {

    }, []);


    return (
        <div>
            
        </div>
    );
};

export default useFetch;